module.exports = {
  // 한 줄 최대 길이 (Airbnb는 100, 대세는 80)
  printWidth: 80,
  // 탭 대신 스페이스 사용
  useTabs: false,
  // 들여쓰기 공간 2칸
  tabWidth: 2,
  // 문장 끝에 세미콜론 사용
  semi: true,
  // 홑따옴표 사용 (true), 쌍따옴표 사용 (false)
  singleQuote: true,
  jsxSingleQuote: true,
  // 객체 리터럴 등에서 후행 쉼표 사용 (es5 또는 all)
  trailingComma: 'all',
  // 화살표 함수의 매개변수가 하나일 때 괄호 생략 (avoid)
  arrowParens: 'avoid',
};
